### Install Rails

```
rvm install 3.0.0
rvm use 3.0.0
gem update bundler
gem install rails -v 6.0.0
```

Install Node & Yarn
> Node comes by default

```
npm install --global yarn
```

### Create new rails project

```
rails new <name>
```
